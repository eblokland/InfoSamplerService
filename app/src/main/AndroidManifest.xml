<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.InfoSamplerService"
        tools:targetApi="31" >
        <activity
            android:name=".DebugDisplay"
            android:exported="true"
            android:label="@string/title_activity_debug_display"
            android:theme="@style/Theme.InfoSamplerService.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <service
            android:name=".EnvironmentSampler"
            android:enabled="true"
            android:exported="true"
            >
            <intent-filter>
                <action android:name=".EnvironmentSampler" />
            </intent-filter>

        </service>
    </application>

</manifest>